{
  "name": "minhareceitaapi",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prestart": "SET NODE_ENV=development&&knex migrate:rollback --all&&knex migrate:latest",
    "start": "SET NODE_ENV=development&&nodemon src/server.ts",
    "pretest": "SET NODE_ENV=test&&knex migrate:rollback --all&&knex migrate:latest",
    "test": "SET NODE_ENV=test&&jest",
    "build": "tsc",
    "production": "npm run build&& node build/src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "celebrate": "^13.0.3",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "knex": "^0.21.6",
    "mysql2": "^2.2.5",
    "uuid": "^8.3.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.8",
    "@types/express": "^4.17.8",
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.8",
    "jest": "^26.5.3",
    "nodemon": "^2.0.5",
    "supertest": "^5.0.0",
    "ts-jest": "^26.4.1",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
